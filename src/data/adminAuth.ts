// Mock-only credential for the prototype admin area. A real backend with
// proper authentication (and per-admin accounts) is required before launch.
export const ADMIN_PASSCODE = 'gibtrades-admin';
